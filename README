# R API for CORELS

More details to come soon when code is cleaned up and so on.

To install, cd into the rcorelsv2 directory and run

# R CMD INSTALL .

Within R, you can use the functions train_from_file() and train() to train the CORELS model.

Predict and print methods coming soon.
